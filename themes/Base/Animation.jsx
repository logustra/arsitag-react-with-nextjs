import { keyframes } from 'styled-components';

const ProgressLeftToRight = keyframes`
    from {
        width: 0;
    }
    to {
        width: 100%;
    }
`

const Animation = {
    ProgressLeftToRight,
}

export default Animation;
